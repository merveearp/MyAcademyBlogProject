@model List<ResultContactInfoDto>
@{
    ViewData["Title"] = "İletişim Bilgileri Yönetimi";
    Layout = "~/Areas/Admin/Views/Admin/AdminLayout.cshtml";
}

<div class="container-fluid">
    <!-- Sayfa Başlığı -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h2 class="mb-1">İletişim Bilgileri</h2>
                    <p class="text-muted">Tüm iletişim bilgilerini görüntüleyin ve yönetin</p>
                </div>
               
                
            </div>
        </div>
    </div>

    <!-- İletişim Bilgileri Tablosu -->
    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-white py-3">
                    <h5 class="mb-0">İletişim Bilgileri Listesi</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th class="px-4">#</th>
                                    <th>Lokasyon</th>
                                    <th>Çalışma Saatleri</th>
                                    <th>Email</th>
                                    <th>Telefon</th>
                                    <th>Oluşturulma Tarihi</th>
                                    <th class="text-center">İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Any())
                                {
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td class="px-4 fw-bold text-muted">@item.Id</td>
                                            <td>
                                                <i class="fas fa-map-marker-alt text-danger me-2"></i>
                                                <span class="text-truncate d-inline-block" style="max-width: 200px;" title="@item.Location">
                                                    @item.Location
                                                </span>
                                            </td>
                                            <td>
                                                <i class="fas fa-clock text-primary me-2"></i>
                                                @item.OpenHours
                                            </td>
                                            <td>
                                                <i class="fas fa-envelope text-info me-2"></i>
                                                <a href="mailto:@item.Email" class="text-decoration-none">@item.Email</a>
                                            </td>
                                            <td>
                                                <i class="fas fa-phone text-success me-2"></i>
                                                <a href="tel:@item.Phone" class="text-decoration-none">@item.Phone</a>
                                            </td>
                                            <td>
                                                <small class="text-muted">
                                                    @item.CreatedDate.ToString("dd.MM.yyyy HH:mm")
                                                </small>
                                            </td>
                                            <td class="text-center">
                                                <div class="btn-group" role="group">
                                                    <a asp-action="Update" asp-route-id="@item.Id"
                                                       class="btn btn-sm btn-outline-primary"
                                                       title="Düzenle">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="7" class="text-center py-5">
                                            <i class="fas fa-inbox fa-3x text-muted mb-3 d-block"></i>
                                            <p class="text-muted mb-0">Henüz kayıt bulunmamaktadır.</p>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                @if (Model != null && Model.Any())
                {
                    <div class="card-footer bg-white py-3">
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="text-muted">Toplam <strong>@Model.Count</strong> kayıt</span>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<style>
    .table td {
        vertical-align: middle;
        padding: 1rem;
    }

    .table th {
        font-weight: 600;
        color: #495057;
        border-bottom: 2px solid #dee2e6;
        padding: 1rem;
    }

    .table tbody tr {
        transition: all 0.3s ease;
    }

        .table tbody tr:hover {
            background-color: #f8f9fa;
            transform: scale(1.01);
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }

    .btn-group .btn {
        padding: 0.375rem 0.75rem;
        margin: 0 2px;
    }

        .btn-group .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.15);
        }

    .card {
        border: none;
        border-radius: 10px;
        overflow: hidden;
    }

    .card-header {
        border-bottom: 2px solid #e9ecef;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        padding: 0.625rem 1.5rem;
        font-weight: 500;
        transition: all 0.3s ease;
    }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

    .text-truncate {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

        (max-width: 768px) {
        .table {
            font-size: 0.875rem;
        }

        .btn-group .btn {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
        }

        .d-flex.justify-content-between {
            flex-direction: column;
            gap: 1rem;
        }

        .text-truncate {
            max-width: 150px !important;
        }
    }

</styl